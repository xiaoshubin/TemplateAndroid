<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- loading dialog -->
    <style name="Theme.Loading.Dialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:textColorPrimaryInverse">@android:color/black</item>
    </style>
    <!-- IOSDialog -->
    <style name="Theme.Ios.Dialog" parent="@android:style/Theme.Dialog">
        <!-- Dialog的windowFrame框为无 -->
        <!-- <item name="android:windowFrame">@null</item> -->
        <!-- 边框 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 半透明 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 设置dialog的背景 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 背景是否模糊显示 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 模糊 -->
        <item name="android:textColorPrimaryInverse">@android:color/black</item>
    </style>
    <!--通用dialog-->
    <style name="dialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <!-- 边框 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 无标题 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 背景透明 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 模糊 -->
        <item name="android:windowNoTitle">true</item>
        <!--<item name="android:windowFrame">@null</item>-->
        <!--// Dialog的windowFrame框为无-->

        <!--<item name="android:windowIsFloating">true</item>-->
        <!--//是否浮现 在Activity之上-->

        <!--<item name="android:windowIsTranslucent">false</item>-->
        <!--//是否半透明-->

        <!--<item name="android:windowNoTitle">true</item>-->


        <!--<item name="android:backgroundDimAmount">0.5</item>-->
        <!--//就是用来控制灰度的值，当为1时，界面除了我们的dialog内容是高亮显示的，dialog以外的区域是黑色的，完全看不到其他内容，系统默认值是0.5-->
    </style>
    <style name="dialog_bottom" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/down_in</item>
        <item name="android:windowExitAnimation">@anim/down_out</item>
    </style>
    <style name="AnimRightInLeftOut">
        <item name="android:windowEnterAnimation">@anim/right_in</item>
        <item name="android:windowExitAnimation">@anim/left_out</item>
    </style>
    <style name="AnimDownInTopOut">
        <item name="android:windowEnterAnimation">@anim/down_in</item>
        <item name="android:windowExitAnimation">@anim/top_out</item>
    </style>
    <!-- 自动换行控件-->
    <declare-styleable name="AutoNewLineLayout">
        <attr name="horizontalSpace" format="dimension|reference"/>
        <attr name="vertivalSpace" format="dimension|reference"/>
    </declare-styleable>
    <!--上线滚动控件-->
    <declare-styleable name="VerticalScrollLayout">
        <attr name="vsl_animDuration" format="integer" />
        <attr name="vsl_sleepTime" format="integer" />
        <attr name="vsl_isCusDuration" format="boolean" />
    </declare-styleable>
        <!-- 橘色的提醒消息 -->
    <style name="OrangeNoticeText">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:background">#FFF7E9</item>
        <item name="android:textColor">#FF7229</item>
        <item name="android:textSize">14sp</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:padding">8dp</item>
        <item name="android:focusable">true</item>
        <item name="android:focusableInTouchMode">true</item>
        <item name="android:singleLine">true</item>
        <item name="android:ellipsize">marquee</item>
        <item name="android:marqueeRepeatLimit">marquee_forever</item>
    </style>
    <declare-styleable name="CirclePercentView">
        <!--圆弧背景色-->
        <attr name="circleBgColor" format="reference" />
        <!--圆弧进度条颜色-->
        <attr name="circleProgressColor" format="reference" />
        <!--圆弧宽度-->
        <attr name="circleRadiu" format="integer" />
        <!--渐变开始颜色-->
        <attr name="circleStartColor" format="reference" />
        <!--渐变结束颜色-->
        <attr name="circleEndColor" format="reference" />
        <!--是否渐变-->
        <attr name="circleIsGradient" format="boolean" />
    </declare-styleable>
    <declare-styleable name="RippleBackground">
        <attr name="rb_color" format="color"/>
        <attr name="rb_strokeWidth" format="float"/>
        <attr name="rb_radius" format="float"/>
        <attr name="rb_duration" format="integer"/>
        <attr name="rb_rippleAmount" format="integer"/>
        <attr name="rb_scale" format="float"/>
        <attr name="rb_type" format="integer"/>
    </declare-styleable>
</resources>